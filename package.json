{
  "name": "fieldflow-project",
  "version": "1.0.0",
  "description": "FieldFlow Field Service Management SaaS - Monorepo Root",
  "private": true,
  "scripts": {
    "dev": "concurrently --kill-others-on-fail --names \"BACKEND,FRONTEND\" --prefix-colors \"cyan,magenta\" \"npm --prefix \\\"fieldflow backend\\\" run dev\" \"npm --prefix \\\"FieldFlow frontend\\\" run dev\"",
    "dev:backend": "npm --prefix \"fieldflow backend\" run dev",
    "dev:frontend": "npm --prefix \"FieldFlow frontend\" run dev",
    "install:all": "npm install && npm --prefix \"fieldflow backend\" install && npm --prefix \"FieldFlow frontend\" install",
    "build": "npm --prefix \"FieldFlow frontend\" run build"
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "express-validator": "^7.1.0",
    "jsonwebtoken": "^9.0.2",
    "mongoose": "^9.9.4",
    "morgan": "^1.10.0"
  },
  "devDependencies": {
    "concurrently": "^9.0.0"
  }
}