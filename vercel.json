{
  "buildCommand": "cd \"FieldFlow frontend\" && npm install && npm run build",
  "outputDirectory": "FieldFlow frontend/dist",
  "framework": "vite",
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "/api/index.js"
    },
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
